<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="16dp"
    android:layout_marginBottom="16dp"
    app:cardCornerRadius="16dp"
    app:cardBackgroundColor="#FFFFFF"
    android:elevation="4dp"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Título editable al crear -->
        <TextView
            android:id="@+id/habitTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Hábito"
            android:textSize="18sp"
            android:textColor="#2E3D49"
            android:textStyle="bold"
            android:paddingBottom="4dp"/>

        <!-- Descripción editable al crear -->
        <TextView
            android:id="@+id/habitDescription"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Descripción del hábito"
            android:textSize="14sp"
            android:textColor="#666666"
            android:paddingBottom="8dp"/>

        <!-- Botón guardar / crear -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/saveHabitButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Crear"
            android:layout_gravity="end"
            android:textColor="#FFFFFF"
            android:backgroundTint="#8BC34A"
            app:cornerRadius="12dp"
            android:elevation="2dp"/>

        <!-- Racha de días -->
        <TextView
            android:id="@+id/habitDayCount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Racha de 0 días"
            android:textSize="14sp"
            android:textColor="#666666"
            android:layout_marginTop="8dp"/>

        <!-- Botón de incrementar días -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/habitDayButton"
            android:layout_width="140dp"
            android:layout_height="wrap_content"
            android:text="Un día más"
            android:layout_gravity="end"
            android:layout_marginTop="8dp"
            app:cornerRadius="12dp"
            android:textColor="#FFFFFF"
            android:backgroundTint="#03A9F4"/>
    </LinearLayout>
</androidx.cardview.widget.CardView>